import { Command } from "./command";

export function showSubCommandWindow(scene: Phaser.Scene, subCommands: Command[], selectedIndex: number) {
  // サブコマンドウインドウの表示処理
}

export function updateSubCommandWindow(scene: Phaser.Scene, subCommands: Command[], selectedIndex: number) {
  // サブコマンドウインドウの内容更新処理
}
